version: '3.1'
services:
  db:
    image: mysql:5.7
    restart: always
    environment:
      MYSQL_ROOT_PASSWORD: bae
      MYSQL_DATABASE: cine
    ports:
      - "3307:3306"
    volumes:
      - ./docker-init:/docker-entrypoint-initdb.d

  adminer:
    image: adminer
    restart: always
    ports:
      - 8101:8080
